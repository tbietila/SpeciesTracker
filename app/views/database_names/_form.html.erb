<%= form_for(@database_name) do |f| %>
  <% if @database_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@database_name.errors.count, "error") %> prohibited this database_name from being saved:</h2>

      <ul>
      <% @database_name.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
